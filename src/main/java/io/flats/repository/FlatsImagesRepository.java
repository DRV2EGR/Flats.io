package io.flats.repository;

import io.flats.entity.FlatsImages;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FlatsImagesRepository extends JpaRepository<FlatsImages, Long> {
}
